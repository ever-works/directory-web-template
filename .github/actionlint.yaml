self-hosted-runner:
  # Self-hosted ARC runner labels used by this repo's Linux CI.
  # Keep in sync with the runs-on: values in .github/workflows/*.
  labels:
    - ever-works-k8s-linux-x64-4
    - ever-works-k8s-linux-x64-8
