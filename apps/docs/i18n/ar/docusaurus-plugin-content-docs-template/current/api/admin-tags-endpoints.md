---
id: admin-tags-endpoints
title: نقاط نهاية واجهة برمجة تطبيقات علامات المشرف
sidebar_label: العلامات الإدارية
sidebar_position: 34
---

# نقاط نهاية واجهة برمجة تطبيقات علامات المشرف

توفر Admin Tag API عمليات CRUD كاملة لإدارة علامات المحتوى. تُستخدم العلامات لتصنيف العناصر وتصفيتها في الدليل. تدعم واجهة برمجة التطبيقات (API) القائمة المقسمة إلى صفحات، والإنشاء بحالات نشطة/غير نشطة، والتحديثات، والحذف، والاسترداد المحلي من ذاكرة التخزين المؤقت للمحتوى. تعمل جميع عمليات الكتابة على إبطال ذاكرة التخزين المؤقت للمحتوى من أجل الرؤية الفورية.

## ملخص الطريق

|الطريقة|المسار|مصادقة|الوصف|
|--------|------|------|-------------|
|`GET`|`/api/admin/tags`|المشرف|علامات القائمة (مرقّمة الصفحات)|
|`POST`|`/api/admin/tags`|المشرف|إنشاء علامة جديدة|
|`GET`|`/api/admin/tags/all`|المشرف|الحصول على جميع العلامات (من ذاكرة التخزين المؤقت للمحتوى)|
|`GET`|`/api/admin/tags/{id}`|المشرف|احصل على علامة واحدة حسب المعرف|
|`PUT`|`/api/admin/tags/{id}`|المشرف|تحديث علامة|
|`DELETE`|`/api/admin/tags/{id}`|المشرف|حذف علامة نهائيًا|

## المصادقة

تتطلب جميع نقاط نهاية إدارة العلامات امتيازات المسؤول:

```typescript
const session = await auth();
if (!session?.user?.isAdmin) {
  return NextResponse.json(
    { success: false, error: 'Unauthorized' },
    { status: 401 }
  );
}
```

## نقاط النهاية

### احصل على `/api/admin/tags`

إرجاع قائمة مرقّمة بجميع العلامات الموجودة في النظام. يتم التحقق من صحة معلمات ترقيم الصفحات باستخدام الأداة المساعدة `validatePaginationParams` المشتركة.

**معلمات الاستعلام:**

|المعلمة|اكتب|الافتراضي|الوصف|
|-----------|------|---------|-------------|
|`page`|عدد صحيح| `1` |رقم الصفحة (الحد الأدنى: 1)|
|`limit`|عدد صحيح| `10` |العناصر لكل صفحة (1--100)|

**الرد (200):**

```json
{
  "success": true,
  "data": {
    "tags": [
      {
        "id": "productivity",
        "name": "Productivity",
        "isActive": true,
        "itemCount": 156,
        "created_at": "2024-01-20T10:30:00.000Z",
        "updated_at": "2024-01-20T10:30:00.000Z"
      },
      {
        "id": "design",
        "name": "Design",
        "isActive": true,
        "itemCount": 89,
        "created_at": "2024-01-19T15:20:00.000Z",
        "updated_at": "2024-01-19T15:20:00.000Z"
      }
    ],
    "total": 45,
    "page": 1,
    "limit": 10,
    "totalPages": 5
  }
}
```

### نشر `/api/admin/tags`

ينشئ علامة جديدة بمعرف واسم وحالة نشطة اختيارية محددة. يبطل ذاكرة التخزين المؤقت للمحتوى عند النجاح.

**نص الطلب:**

```json
{
  "id": "artificial-intelligence",
  "name": "Artificial Intelligence",
  "isActive": true
}
```

|الميدان|اكتب|مطلوب|الوصف|
|-------|------|----------|-------------|
|`id`|سلسلة|نعم|معرف سبيكة صديق لعنوان URL|
|`name`|سلسلة|نعم|اسم العلامة الذي يمكن قراءته بواسطة الإنسان (2-50 حرفًا)|
|`isActive`|منطقية|لا|ما إذا كانت العلامة نشطة (الافتراضي: `true`)|

**قواعد التحقق:**
- مطلوب كل من `id` و`name`
- يجب أن يتراوح اسم العلامة بين 2 و50 حرفًا
- يجب أن يكون معرف العلامة فريدًا عبر جميع العلامات الموجودة
- يجب أن يكون اسم العلامة فريدًا عبر جميع العلامات الموجودة

**الرد (201):**

```json
{
  "success": true,
  "tag": {
    "id": "artificial-intelligence",
    "name": "Artificial Intelligence",
    "isActive": true,
    "itemCount": 0,
    "created_at": "2024-01-20T10:30:00.000Z",
    "updated_at": "2024-01-20T10:30:00.000Z"
  }
}
```

### احصل على `/api/admin/tags/all`

إرجاع كافة العلامات من ذاكرة التخزين المؤقت للمحتوى لمنطقة معينة. تقرأ نقطة النهاية هذه من طبقة المحتوى المخزن مؤقتًا بدلاً من قاعدة البيانات، مما يجعلها مناسبة لملء محددات العلامات في واجهة مستخدم المسؤول.

**معلمات الاستعلام:**

|المعلمة|اكتب|الافتراضي|الوصف|
|-----------|------|---------|-------------|
|`locale`|سلسلة|`"en"`|رمز اللغة لاسترجاع المحتوى|

**الرد (200):**

```json
{
  "success": true,
  "data": [
    {
      "id": "productivity",
      "name": "Productivity",
      "isActive": true,
      "itemCount": 156
    }
  ]
}
```

### احصل على `/api/admin/tags/{id}`

استرداد علامة واحدة بواسطة معرفها الفريد مع تفاصيل كاملة بما في ذلك إحصائيات الاستخدام.

** معلمات المسار: **

|المعلمة|اكتب|الوصف|
|-----------|------|-------------|
|`id`|سلسلة|علامة معرف فريد|

**الرد (200):**

```json
{
  "success": true,
  "data": {
    "id": "productivity",
    "name": "Productivity",
    "isActive": true,
    "itemCount": 156,
    "created_at": "2024-01-20T10:30:00.000Z",
    "updated_at": "2024-01-20T10:30:00.000Z"
  }
}
```

### ضع `/api/admin/tags/{id}`

يقوم بتحديث اسم العلامة و/أو الحالة النشطة. لا يمكن تغيير معرف العلامة بعد الإنشاء. يبطل ذاكرة التخزين المؤقت للمحتوى عند النجاح.

** معلمات المسار: **

|المعلمة|اكتب|الوصف|
|-----------|------|-------------|
|`id`|سلسلة|علامة معرف فريد|

**نص الطلب:**

```json
{
  "name": "Productivity & Efficiency",
  "isActive": true
}
```

|الميدان|اكتب|مطلوب|الوصف|
|-------|------|----------|-------------|
|`name`|سلسلة|نعم|تم تحديث اسم عرض العلامة|
|`isActive`|منطقية|لا|تم تحديث الحالة النشطة|

**الرد (200):**

```json
{
  "success": true,
  "data": {
    "id": "productivity",
    "name": "Productivity & Efficiency",
    "isActive": true,
    "itemCount": 156,
    "created_at": "2024-01-20T10:30:00.000Z",
    "updated_at": "2024-01-20T16:45:00.000Z"
  },
  "message": "Tag updated successfully"
}
```

### احذف `/api/admin/tags/{id}`

حذف علامة من النظام نهائيًا. يؤدي هذا أيضًا إلى إزالة العلامة من كافة العناصر المرتبطة. يبطل ذاكرة التخزين المؤقت للمحتوى عند النجاح.

** معلمات المسار: **

|المعلمة|اكتب|الوصف|
|-----------|------|-------------|
|`id`|سلسلة|علامة معرف فريد|

**الرد (200):**

```json
{
  "success": true,
  "message": "Tag deleted successfully"
}
```

::: الحذر
يعد حذف العلامة أمرًا نهائيًا ولا يمكن التراجع عنه. ستتم إزالة كافة ارتباطات علامة العنصر للعلامة المحذوفة. فكر في إلغاء تنشيط العلامة (ضبط `isActive` على `false` عبر PUT) بدلاً من ذلك إذا كنت تريد الحفاظ على سلامة البيانات.
:::

## نموذج بيانات العلامة

|الميدان|اكتب|لاغية|الوصف|
|-------|------|----------|-------------|
|`id`|سلسلة|لا|معرف فريد صديق لعنوان URL|
|`name`|سلسلة|لا|اسم العرض الذي يمكن قراءته بواسطة الإنسان|
|`isActive`|منطقية|لا|ما إذا كان يمكن تعيين العلامة للعناصر|
|`itemCount`|عدد صحيح|لا|عدد العناصر التي تستخدم هذه العلامة|
|`created_at`|التاريخ والوقت|لا|الطابع الزمني للإنشاء|
|`updated_at`|التاريخ والوقت|لا|الطابع الزمني للتحديث الأخير|

## رموز الخطأ

|الحالة|خطأ|السبب|
|--------|-------|-------|
| `400` |مطلوب معرف العلامة والاسم|الحقول المطلوبة مفقودة عند الإنشاء|
| `400` |اسم العلامة مطلوب|الاسم مفقود في التحديث|
| `400` |يجب أن يتراوح اسم العلامة بين 2 و50 حرفًا|فشل التحقق من صحة طول الاسم|
| `400` |معلمة الصفحة/الحد غير صالحة|معلمة ترقيم الصفحات خارج النطاق|
| `401` |غير مصرح به|جلسة مفقودة أو غير إدارية|
| `404` |لم يتم العثور على العلامة|لا توجد علامة بالمعرف المحدد|
| `409` |العلامة ذات المعرف موجودة بالفعل|معرف مكرر عند الإنشاء|
| `409` |العلامة بالاسم موجودة بالفعل|اسم مكرر عند الإنشاء/التحديث|
| `500` |فشل جلب/إنشاء/تحديث/حذف العلامة|خطأ في الخادم أو قاعدة البيانات|

## إبطال ذاكرة التخزين المؤقت

جميع عمليات الكتابة (الإنشاء والتحديث والحذف) تستدعي `invalidateContentCaches()` لضمان انعكاس تغييرات العلامة على الفور في المحتوى العام:

```typescript
await invalidateContentCaches();
```

يؤدي هذا إلى مسح ذاكرة التخزين المؤقت للمحتوى في الذاكرة وأي ذاكرة تخزين مؤقت على مستوى CDN قد تكون نشطة.

## مصادر البيانات

تستخدم واجهة برمجة تطبيقات العلامات مصدرين مختلفين للبيانات اعتمادًا على نقطة النهاية:

|نقطة النهاية|مصدر البيانات|حالة الاستخدام|
|----------|------------|----------|
|`GET /api/admin/tags`|`tagRepository` (قاعدة البيانات)|إدارة المشرف مع ترقيم الصفحات|
|`POST /api/admin/tags`|`tagRepository` (قاعدة البيانات)|إنشاء علامات جديدة|
|`GET /api/admin/tags/all`|`getCachedItems()` (ذاكرة التخزين المؤقت للمحتوى)|محددات القائمة المنسدلة، وعمليات البحث السريعة|
|`GET /api/admin/tags/{id}`|`tagRepository` (قاعدة البيانات)|عرض تفصيلي للعلامة|
|`PUT /api/admin/tags/{id}`|`tagRepository` (قاعدة البيانات)|تحديث خصائص العلامة|
|`DELETE /api/admin/tags/{id}`|`tagRepository` (قاعدة البيانات)|إزالة العلامات|

## الوثائق ذات الصلة

- [نظرة عامة على نقاط نهاية المشرف](./admin-endpoints.md)
- [نقاط نهاية فئات المسؤول](./admin-categories-endpoints.md) - نمط مماثل لإدارة الفئات
- [أنماط الاستجابة](./response-patterns.md)
- [التحقق من صحة الطلب](./request-validation.md)
