---
id: queries
title: הפניה לשאילתות מסד נתונים
sidebar_label: שאילתות
sidebar_position: 2
---

# הפניה לשאילתות מסד נתונים

הספרייה `lib/db/queries/` מכילה 23+ מודולי שאילתות המאורגנים לפי תחום. כל מודול מקפל שאילתות Drizzle ORM עבור אזור תכונה ספציפי, בהתאם לעקרון האחריות היחידה.

## סקירת מודול

כל מודולי השאילתה מיוצאים מ-`lib/db/queries/index.ts` לייבוא נוח:

```typescript
import { getUser, getUserByEmail } from '@/lib/db/queries';
```

## מודולי שאילתה

### activity.queries.ts

רישום פעילויות ושליפה עבור מערכת שבילי הביקורת.

**פונקציות מפתח:**
- רישום פעילויות משתמש (כניסה, הרשמה, שינויים בחשבון)
- שאילתה היסטוריית פעילות לפי משתמש או טווח תאריכים

### auth.queries.ts

פעולות מסד נתונים הקשורות לאימות.

**פונקציות מפתח:**
- מצא משתמש בדוא"ל לצורך אימות אישורים
- צור ואמת אסימוני איפוס סיסמה
- נהל אסימוני אימות

### client.queries.ts

מודול השאילתות הגדול ביותר (37KB), המטפל בכל הפעולות מול הלקוח.

**פונקציות מפתח:**
- פרופיל לקוח פעולות CRUD
- הגשת פריטי לקוחות וניהול
- צבירת נתונים של לוח המחוונים של הלקוח
- חיפוש וסנן נתוני לקוח
- שאילתות רישום עם עימוד

### comment.queries.ts

הערות פעולות מערכת.

**פונקציות מפתח:**
- צור, עדכן ומחק תגובות בצורה רכה
- אחזר הערות לפי פריט עם עימוד
- שאילתות ניהול תגובות (אדמין)
- צבירת דירוג

### company.queries.ts

שאילתות הנהלת החברה.

**פונקציות מפתח:**
- פעילות CRUD של החברה
- חיפוש וסינון חברה
- ניהול איגוד פריט-חברה
- סטטיסטיקה וניתוח של החברה

### dashboard.queries.ts

צבירת נתוני לוח מחוונים עבור לוחות מחוונים של מנהל מערכת ולקוח.

**פונקציות מפתח:**
- נתונים סטטיסטיים של לוח המחוונים של מנהל המערכת (סה"כ משתמשים, פריטים, הכנסה)
- נתונים סטטיסטיים של לוח המחוונים של לקוחות (הגשות, צפיות, מעורבות)
- נתוני סדרות זמן עבור תרשימים
- סיכומי פעילות

### engagement.queries.ts

מדדי מעורבות מצטברים על פני צפיות, הצבעות, מועדפים והערות.

**פונקציות מפתח:**
- קבל ציוני מעורבות עבור פריטים
- ספירת צפיות מצטברת
- חשב מדדי פופולריות
- דירוג המעורבות

### integration-mapping.queries.ts

פעולות מיפוי אינטגרציה של CRM.

**פונקציות מפתח:**
- צור ועדכן מיפויי אינטגרציה
- חפש מזהי CRM מתוך מזהי Ever ולהיפך
- עקוב אחר חותמות זמן של סנכרון וגיבוב גרסאות
- פעולות מיפוי בכמות גדולה

### item.queries.ts

שאילתות פריט ליבה (פריטים מאוחסנים ב-Git, אך מעקב אחר מטא נתונים במסד הנתונים).

**פונקציות מפתח:**
- פעולות מטא נתונים של פריט
- מעקב אחר תצוגת פריט
- נתוני מעורבות פריטים

### item-audit.queries.ts

פעולות יומן ביקורת פריט.

**פונקציות מפתח:**
- הקלט פעולות של יצירה, עדכון, מחיקה ובדיקה
- שאילתה היסטוריית ביקורת עבור פריטים ספציפיים
- סנן יומני ביקורת לפי סוג פעולה, מבצע או טווח תאריכים

### item-view.queries.ts

מעקב וניתוח של תצוגת פריט.

**פונקציות מפתח:**
- הקלט צפיות יומיות ייחודיות (ביטול כפילויות לפי מזהה צופה ותאריך)
- ספירת תצוגת שאילתה לפי פריט וטווח תאריכים
- הצג צבירה של ניתוח

### location-index.queries.ts

חיפוש מבוסס מיקום ואינדקס.

**פונקציות מפתח:**
- שאילתות גיאוגרפיות עבור פריטים קרובים
- ניהול אינדקס מיקום
- חישובי מרחק
- חיפוש מבוסס מיקום עם מסננים

### moderation.queries.ts

מערכת ניהול תוכן.

**פונקציות מפתח:**
- יצירה וניהול של דוחות תוכן
- עדכן את מצב הדוח והרזולוציה
- הקלט פעולות ניהול
- סטטיסטיקת ניהול וניהול תורים

### newsletter.queries.ts

ניהול מנוי לניוזלטר.

**פונקציות מפתח:**
- פעולות הרשמה וביטול הרשמה
- בדוק את סטטוס המנוי
- רשימת מנויים פעילים
- עקוב אחר היסטוריית שליחת אימייל

### payment.queries.ts

פעולות מסד נתונים הקשורות לתשלום.

**פונקציות מפתח:**
- ניהול ספקי תשלומים
- קישור חשבון תשלום
- רישום עסקה
- שאילתות היסטוריית תשלומים

### report.queries.ts

שאילתות מערכת דיווח תוכן.

**פונקציות מפתח:**
- צור דוחות (פריט או הערה)
- רשימת דוחות עם מסננים ועימוד
- עדכון סטטוס דוח
- דוח ניתוח

### subscription.queries.ts

ניהול מחזור חיים של מנוי (17KB).

**פונקציות מפתח:**
- צור ועדכן מנויים
- מעברי סטטוס מנוי
- הקלטת היסטוריית מנויים
- מצא מנויים לפי מזהה משתמש או ספק
- פעולות חידוש וביטול
- ניתוח מנויים

### survey.queries.ts

תפעול מערכת הסקר.

**פונקציות מפתח:**
- סקר פעולות CRUD
- הקלטת תגובת הסקר
- צבירת תגובה וניתוח
- ניהול סטטוס הסקר (טיוטה, פורסם, סגור)

### user.queries.ts

שאילתות לניהול משתמשים.

**פונקציות מפתח:**
- פעולות CRUD של משתמש
- חיפוש וסינון משתמשים
- ניהול תפקידי משתמש
- מחיקת חשבון (מחיקה רכה)

### vote.queries.ts

תפעול מערכת ההצבעה.

**פונקציות מפתח:**
- צור, עדכן והסר הצבעות
- בדוק הצבעות קיימות עבור זוג פריט משתמש
- ספירת קולות מצטברת לפי פריט
- החלפת סוג הצבעה (הצבעה למעלה/למטה)

## כלי עזר משותפים

### types.ts

סוגי TypeScript משותפים המשמשים בין מודולי שאילתה:

```typescript
// Common query parameter types
export interface PaginationParams {
  page: number;
  limit: number;
}
```

### utils.ts

פונקציות שירות משותפות לבניית שאילתות:

- עוזרי עימוד (חישוב היסט, עיצוב תוצאה)
- בוני מסננים נפוצים
- עוזרי שברי SQL

## דפוסי שאילתות

### דפוס שאילתה סטנדרטי

כל מודולי השאילתה עוקבים אחר דפוס עקבי:

```typescript
import { db } from '../drizzle';
import { eq, desc, and, sql } from 'drizzle-orm';
import { tableName } from '../schema';

export async function getItemById(id: string) {
  const result = await db
    .select()
    .from(tableName)
    .where(eq(tableName.id, id))
    .limit(1);
  return result[0] || null;
}
```

### שאילתות מדורגות

מודולים רבים מיישמים שאילתות מעומדות:

```typescript
export async function getItems(page: number, limit: number) {
  const offset = (page - 1) * limit;
  const [items, countResult] = await Promise.all([
    db.select().from(tableName)
      .orderBy(desc(tableName.createdAt))
      .limit(limit)
      .offset(offset),
    db.select({ count: sql<number>`count(*)` })
      .from(tableName),
  ]);
  return {
    items,
    total: Number(countResult[0].count),
    page,
    limit,
  };
}
```

### שאילתות צבירה

מודולי המעורבות ולוח המחוונים משתמשים בצבירה של SQL:

```typescript
export async function getEngagementScore(itemId: string) {
  const result = await db.execute(sql`
    SELECT
      COALESCE(v.vote_count, 0) as votes,
      COALESCE(c.comment_count, 0) as comments,
      COALESCE(f.favorite_count, 0) as favorites,
      COALESCE(iv.view_count, 0) as views
    FROM ...
  `);
  return result;
}
```

## אמנת יבוא

ייבוא פונקציות שאילתה דרך ייצוא החבית:

```typescript
// Preferred: import from barrel
import { getUser, createSubscription, getVotesByItem } from '@/lib/db/queries';

// Also valid: import from specific module
import { getUser } from '@/lib/db/queries/user.queries';
```
