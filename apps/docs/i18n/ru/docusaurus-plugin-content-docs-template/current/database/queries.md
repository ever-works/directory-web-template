---
id: queries
title: Справочник по запросам к базе данных
sidebar_label: Запросы
sidebar_position: 2
---

# Справочник по запросам к базе данных

Каталог `lib/db/queries/` содержит более 23 модулей запросов, организованных по доменам. Каждый модуль инкапсулирует запросы Drizzle ORM для определенной области функций в соответствии с принципом единой ответственности.

## Обзор модуля

Все модули запросов экспортируются из `lib/db/queries/index.ts` для удобного импорта:

```typescript
import { getUser, getUserByEmail } from '@/lib/db/queries';
```

## Модули запросов

### Activity.queries.ts

Регистрация активности и поиск для системы контрольного журнала.

**Основные функции:**
- Регистрируйте действия пользователей (вход, регистрация, изменения учетной записи)
- Запрос истории активности по пользователю или диапазону дат

### auth.queries.ts

Операции с базой данных, связанные с аутентификацией.

**Основные функции:**
- Найти пользователя по электронной почте для аутентификации учетных данных
- Создание и проверка токенов сброса пароля
- Управление токенами проверки

### client.queries.ts

Самый большой модуль запросов (37 КБ), обрабатывающий все операции с клиентом.

**Основные функции:**
- Операции CRUD профиля клиента
- Отправка и управление клиентскими элементами
- Агрегация данных панели управления клиента
- Поиск и фильтрация данных о клиентах
- Запросы на листинг с разбивкой на страницы

### комментарий.queries.ts

Комментарии к работе системы.

**Основные функции:**
- Создание, обновление и обратимое удаление комментариев
- Получение комментариев по элементам с нумерацией страниц
- Запросы на модерацию комментариев (администратор)
- Агрегация рейтинга

### компания.queries.ts

Вопросы руководства компании.

**Основные функции:**
- Деятельность компании CRUD
- Поиск и фильтрация компаний
- Управление ассоциацией «Товар-компания»
- Статистика и аналитика компании

### приборная панель.queries.ts

Агрегация данных информационной панели для панелей администратора и клиента.

**Основные функции:**
- Статистика панели администратора (общее количество пользователей, элементы, доход)
- Статистика клиентской панели (отправки, просмотры, вовлеченность)
- Данные временных рядов для диаграмм
- Сводные данные о деятельности

### Engagement.queries.ts

Совокупные показатели вовлеченности по просмотрам, голосам, избранному и комментариям.

**Основные функции:**
- Получайте оценки вовлеченности для элементов
- Общее количество просмотров
- Рассчитать показатели популярности
- Рейтинги вовлеченности

### интеграция-mapping.queries.ts

Операции по отображению интеграции CRM.

**Основные функции:**
- Создание и обновление сопоставлений интеграции
- Найдите идентификаторы CRM в идентификаторах Ever и наоборот.
- Отслеживайте временные метки синхронизации и хэши версий.
- Массовые картографические операции

### item.queries.ts

Запросы основных элементов (элементы хранятся в Git, но метаданные отслеживаются в базе данных).

**Основные функции:**
- Операции с метаданными элемента
- Отслеживание просмотров товаров
- Данные о взаимодействии с объектом

### элемент-audit.queries.ts

Операции журнала аудита элементов.

**Основные функции:**
- Записывайте создание, обновление, удаление элементов и просмотрите действия.
- Запросить историю аудита для конкретных элементов
- Фильтрация журналов аудита по типу действия, исполнителю или диапазону дат.

### элемент-view.queries.ts

Отслеживание и аналитика просмотров товаров.

**Основные функции:**
- Записывайте уникальные ежедневные просмотры (дедублированные по идентификатору зрителя и дате).
- Количество просмотров запроса по элементу и диапазону дат
- Просмотр агрегированной аналитики

### местоположение-index.queries.ts

Поиск и индексирование на основе местоположения.

**Основные функции:**
- Геопространственные запросы для близлежащих объектов
- Управление индексом местоположения
- Расчеты расстояний
- Поиск по местоположению с фильтрами

### модерация.queries.ts

Система модерации контента.

**Основные функции:**
- Создание отчетов по контенту и управление ими
- Обновить статус и разрешение отчета
- Запись действий модератора
- Статистика модерации и управление очередью

### информационная рассылка.queries.ts

Управление подпиской на рассылку.

**Основные функции:**
- Операции подписки и отмены подписки
- Проверить статус подписки
- Список активных подписчиков
- Отслеживать историю отправки электронной почты

### платеж.queries.ts

Операции с базой данных, связанные с платежами.

**Основные функции:**
- Управление платежными провайдерами
- Привязка платежного счета
- Запись транзакций
- Запросы истории платежей

### report.queries.ts

Запросы системы отчетности по контенту.

**Основные функции:**
- Создание отчетов (элемент или комментарий)
- Получение списка отчетов с фильтрами и нумерацией страниц
- Обновить статус отчета
- Аналитика отчетов

### подписка.queries.ts

Управление жизненным циклом подписки (17 КБ).

**Основные функции:**
- Создание и обновление подписок
- Изменение статуса подписки
- Запись истории подписок
- Поиск подписок по идентификатору пользователя или поставщика
- Операции продления и отмены
- Аналитика подписки

### Survey.queries.ts

Операции с обзорной системой.

**Основные функции:**
- Исследование операций CRUD
- Запись ответов на опрос
- Агрегация ответов и аналитика
- Управление статусом опроса (черновик, опубликовано, закрыто)

### user.queries.ts

Запросы по управлению пользователями.

**Основные функции:**
- Пользовательские CRUD-операции
- Поиск и фильтрация пользователей
- Управление ролями пользователей
- Удаление учетной записи (мягкое удаление)

### voice.queries.ts

Работа системы голосования.

**Основные функции:**
- Создание, обновление и удаление голосов
- Проверьте существующие голоса для пары «пользователь-элемент»
- Совокупный подсчет голосов по пунктам
- Переключение типа голосования (за/против)

## Общие утилиты

### типы.ts

Общие типы TypeScript, используемые в модулях запросов:

```typescript
// Common query parameter types
export interface PaginationParams {
  page: number;
  limit: number;
}
```

### utils.ts

Общие служебные функции для построения запросов:

- Помощники по нумерации страниц (вычисление смещения, форматирование результатов)
- Общие конструкторы фильтров
- Помощники фрагментов SQL

## Шаблоны запросов

### Стандартный шаблон запроса

Все модули запросов следуют единому шаблону:

```typescript
import { db } from '../drizzle';
import { eq, desc, and, sql } from 'drizzle-orm';
import { tableName } from '../schema';

export async function getItemById(id: string) {
  const result = await db
    .select()
    .from(tableName)
    .where(eq(tableName.id, id))
    .limit(1);
  return result[0] || null;
}
```

### Разбивка на страницы запросов

Многие модули реализуют запросы с разбиением на страницы:

```typescript
export async function getItems(page: number, limit: number) {
  const offset = (page - 1) * limit;
  const [items, countResult] = await Promise.all([
    db.select().from(tableName)
      .orderBy(desc(tableName.createdAt))
      .limit(limit)
      .offset(offset),
    db.select({ count: sql<number>`count(*)` })
      .from(tableName),
  ]);
  return {
    items,
    total: Number(countResult[0].count),
    page,
    limit,
  };
}
```

### Агрегационные запросы

Модули взаимодействия и информационной панели используют агрегацию SQL:

```typescript
export async function getEngagementScore(itemId: string) {
  const result = await db.execute(sql`
    SELECT
      COALESCE(v.vote_count, 0) as votes,
      COALESCE(c.comment_count, 0) as comments,
      COALESCE(f.favorite_count, 0) as favorites,
      COALESCE(iv.view_count, 0) as views
    FROM ...
  `);
  return result;
}
```

## Импортная конвенция

Импортируйте функции запроса через экспорт ствола:

```typescript
// Preferred: import from barrel
import { getUser, createSubscription, getVotesByItem } from '@/lib/db/queries';

// Also valid: import from specific module
import { getUser } from '@/lib/db/queries/user.queries';
```
